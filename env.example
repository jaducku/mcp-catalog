# MCP 카탈로그 애플리케이션 환경변수 설정
# Pod 배포 시 ConfigMap으로 이 값들을 주입합니다.

# ================================
# API 엔드포인트 설정
# ================================

# MCP 서버 헬스체크 API URL (필수)
NEXT_PUBLIC_HEALTH_CHECK_API_URL=http://mcp-health-check-service:8080

# MCP 도구 조회 API URL (필수)
NEXT_PUBLIC_TOOLS_API_URL=http://mcp-tools-service:8080

# ================================
# 타임아웃 및 재시도 설정
# ================================

# 헬스체크 타임아웃 (밀리초, 기본값: 10000)
NEXT_PUBLIC_HEALTH_CHECK_TIMEOUT=10000

# 헬스체크 재시도 횟수 (기본값: 3)
NEXT_PUBLIC_HEALTH_CHECK_RETRIES=3

# WebSocket 연결 타임아웃 (밀리초, 기본값: 10000)
NEXT_PUBLIC_WEBSOCKET_TIMEOUT=10000

# ================================
# 디버그 및 로깅 설정
# ================================

# 디버그 모드 활성화 (true/false, 기본값: false)
NEXT_PUBLIC_DEBUG=false

# Node.js 환경 (development/production, 기본값: production)
NODE_ENV=production

# ================================
# Kubernetes ConfigMap 예시
# ================================
# 
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: mcp-catalog-config
#   namespace: default
# data:
#   NEXT_PUBLIC_HEALTH_CHECK_API_URL: "http://mcp-health-check-service:8080"
#   NEXT_PUBLIC_TOOLS_API_URL: "http://mcp-tools-service:8080"
#   NEXT_PUBLIC_HEALTH_CHECK_TIMEOUT: "15000"
#   NEXT_PUBLIC_HEALTH_CHECK_RETRIES: "3"
#   NEXT_PUBLIC_WEBSOCKET_TIMEOUT: "15000"
#   NEXT_PUBLIC_DEBUG: "false"
#   NODE_ENV: "production"
# 
# ---
# 
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: mcp-catalog
# spec:
#   template:
#     spec:
#       containers:
#       - name: mcp-catalog
#         image: mcp-catalog:latest
#         envFrom:
#         - configMapRef:
#             name: mcp-catalog-config
#         ports:
#         - containerPort: 3000 